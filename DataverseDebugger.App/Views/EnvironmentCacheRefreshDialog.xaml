<Window x:Class="DataverseDebugger.App.Views.EnvironmentCacheRefreshDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Refresh Environment Cache"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Loaded="OnLoaded"
        MinWidth="540"
        Background="{DynamicResource ThemeBackgroundBrush}">
    <Border Margin="12"
            Background="{DynamicResource ThemeSurfaceBrush}"
            BorderBrush="{DynamicResource ThemeBorderBrush}"
            BorderThickness="1"
            CornerRadius="8"
            Padding="12">
        <StackPanel>
            <TextBlock x:Name="HeaderText"
                       Text="Refreshing..."
                       FontSize="16"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource ThemeTextPrimaryBrush}"/>
            <TextBlock x:Name="DescriptionText"
                       Margin="0 4 0 12"
                       Text="Updating cached data for this environment."
                       Foreground="{DynamicResource ThemeTextSecondaryBrush}"
                       TextWrapping="Wrap"/>
            <ProgressBar x:Name="ProgressBar"
                         Height="6"
                         IsIndeterminate="True"
                         Visibility="Collapsed"
                         Margin="0 0 0 12"
                         Foreground="{DynamicResource ThemeAccentBrush}"
                         Background="{DynamicResource ThemeSurfaceLightBrush}"/>
            <TextBlock x:Name="StatusText"
                       Foreground="{DynamicResource ThemeTextSecondaryBrush}"
                       Text="Starting..."
                       TextWrapping="Wrap"/>
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0 16 0 0">
                <Button x:Name="CloseButton"
                        Content="Close"
                        Width="90"
                        Height="28"
                        Style="{DynamicResource ModernToolbarButton}"
                        Visibility="Collapsed"
                        Click="OnCloseClick"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
