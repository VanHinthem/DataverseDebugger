<UserControl x:Class="DataverseDebugger.App.Views.PluginsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
    <UserControl.Resources>
        <SolidColorBrush x:Key="PluginsWarningBrush" Color="#FFB300"/>
    </UserControl.Resources>
    <Grid Background="{DynamicResource ThemeBackgroundBrush}">
        <Border Background="{DynamicResource ThemeSurfaceBrush}" CornerRadius="8" Padding="12" BorderBrush="{DynamicResource ThemeBorderBrush}" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel x:Name="StatusPanel" Grid.Row="0" Margin="0 0 0 8">
                    <StackPanel x:Name="StatusPrimaryRow" Orientation="Horizontal" Margin="0 0 0 4">
                        <TextBlock x:Name="StatusIcon" Text="i" Foreground="{DynamicResource ThemeTextSecondaryBrush}" FontSize="12" Margin="0 1 6 0"/>
                        <TextBlock x:Name="StatusText" Foreground="{DynamicResource ThemeTextSecondaryBrush}" TextWrapping="Wrap"/>
                    </StackPanel>
                    <TextBlock x:Name="StatusDetailText" Foreground="{DynamicResource ThemeTextMutedBrush}" TextWrapping="Wrap" Visibility="Collapsed"/>
                    <TextBlock x:Name="StatusWarningText" Foreground="{StaticResource PluginsWarningBrush}" TextWrapping="Wrap" Visibility="Collapsed" Margin="0 4 0 0"/>
                </StackPanel>

                <Border Grid.Row="1" Background="{DynamicResource ThemeSurfaceBrush}" BorderBrush="{DynamicResource ThemeBorderBrush}" BorderThickness="1" CornerRadius="4" Padding="8">
                    <TreeView x:Name="PluginsTree"
                              ItemsSource="{Binding PluginTreeItems}"
                              BorderThickness="0"
                              Background="Transparent">
                        <TreeView.ItemContainerStyle>
                            <Style TargetType="TreeViewItem">
                                <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
                            </Style>
                        </TreeView.ItemContainerStyle>
                        <TreeView.ItemTemplate>
                            <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Icon}"
                                               FontFamily="Segoe UI Emoji"
                                               Foreground="{DynamicResource ThemeTextSecondaryBrush}"
                                               Margin="0 0 6 0"/>
                                    <TextBlock Text="{Binding Title}" Foreground="{DynamicResource ThemeTextPrimaryBrush}"/>
                                </StackPanel>
                            </HierarchicalDataTemplate>
                        </TreeView.ItemTemplate>
                    </TreeView>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
